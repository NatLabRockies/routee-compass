[toolchain]
# Pinned to match the `rust` version in pyproject.toml's pixi environment so that
# `pixi run check_rust`, local `cargo`, and CI all use the same clippy/rustc and
# catch the same lints. Bump this and the pyproject.toml pin together.
channel = "1.96.1"
components = ["rustfmt", "clippy"]
